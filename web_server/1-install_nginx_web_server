#!/usr/bin/env bash
# Installs Nginx, updates the index page, and starts the service

apt-get update -y
apt-get install -y nginx
echo "Holberton School" > /var/www/html/index.html
service nginx restart
sleep 2
